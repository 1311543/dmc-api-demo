pipeline {
    agent any
    stages {
        stage("Test") {
            steps {
                parallel(
                        unit: {
                            echo "pruebas unitarias"
                        },
                        syntax: {
                            echo "pruebas de sintaxis"
                        },
                        linter: {
                            echo "flake8"
                        }
                )
            }
        }
    }
}
